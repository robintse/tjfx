Ext.define('MyApp.view.HomePanel', {    alternateClassName: 'home',    extend: 'Ext.Container',    xtype: 'homePanel',    requires: [        'MyApp.view.Home'    ],    config: {        id: 'homePanel',        layout: {            type: 'card'        },        hideAnimation: {            type: 'slideOut',            direction: 'right',            reveal: true        },        showAnimation: {            type: 'slideIn',            direction: 'left',            cover: true        },        items: [            {                xtype: 'toolbar',                docked: 'top',                itemId: 'toolbar',                title: '税务移动查询',                items: [                    {                        xtype: 'button',                        hidden: true,                        hideAnimation: 'fadeOut',                        itemId: 'backButton',                        showAnimation: {                            type: 'slide',                            direction: 'right'                        },                        ui: 'back',                        text: '返回'                    }                ],            },            {                xtype: 'home'            }        ],        listeners: [            {                fn: 'onBackButtonTap',                event: 'tap',                delegate: '#backButton'            }        ]    },    onBackButtonTap: function (button, e, eOpts) {        var home = Ext.getCmp('home');        var itemTpl = Ext.getCmp('itemTpl');        var chart = Ext.getCmp('chart');        var bigDataList = Ext.getCmp('bigDataList');        var toolbar = Ext.ComponentQuery.query('#toolbar')[0];        if (null != itemTpl && !itemTpl.isHidden()) {            itemTpl.hide();            home.show();            button.hide();            toolbar.setTitle('税务移动查询');        } else if (null != chart && !chart.isHidden()) {            itemTpl.show();            chart.hide();            toolbar.setTitle('大屏数据');        } else if (null != bigDataList && !bigDataList.isHidden()) {            itemTpl.show();            bigDataList.hide();            toolbar.setTitle('税务移动查询');        }    }});